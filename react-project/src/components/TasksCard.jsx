import './Card.css'


export function TasksCard({ project }) {
    return (
        <>
        <h1>Hi</h1>
            {/* <div className="horizontal-card">
                <h2>Team Name: {project?.proj_name} - Team Size: {project?.team_size} - Estimated Completion Time: {project?.completion_time}</h2>
            </div>
            <div> <button onClick={() => setShowComponent(true)}>View Tasks</button>
      {showComponent && <NewComponent />}</div> */}
        </>
    )
}